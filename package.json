{
  "name": "gamehub-forum",
  "version": "1.0.0",
  "private": true,
  "description": "GameHub Forum - edukacyjna aplikacja Full Stack dla graczy.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend",
    "check": "npm run check --prefix frontend && npm run check --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
